name: Build & Verify Windows Setup

# 触发方式（任选其一都会跑这套打包+验证）：
#   - 推 v* 标签（正式发版）
#   - 手动触发（workflow_dispatch，可传版本号）
#   - 推 master/main（可选，防回归）
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（如 1.0.4），留空则从代码读取'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build-and-verify:
    name: Build Setup.exe + verify
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: 校验 CRLF（cmd 必须 CRLF，否则 cmd.exe 解析括号块会失败）
        shell: bash
        run: |
          for f in scripts/*.cmd; do
            # Git Bash 的 GNU grep 读文本文件会剥离 CR，grep $'\r' 对 CRLF 文件恒不命中，
            # 必须用字节统计：每个换行必须是 CRLF（允许末行无换行），出现纯 LF 行即不合格。
            cr=$(tr -cd '\r' < "$f" | wc -c)
            lf=$(tr -cd '\n' < "$f" | wc -c)
            if [ "$lf" -gt "$cr" ]; then
              echo "::error::$f 存在 $((lf - cr)) 个纯 LF 行，cmd.exe 解析括号块会失败，请转换为 CRLF"
              exit 1
            fi
          done
          echo "所有 .cmd 均为 CRLF ✓"

      - name: 解析版本号
        id: ver
        shell: bash
        run: |
          VER="${{ github.event.inputs.version }}"
          if [ -z "$VER" ]; then
            VER=$(grep -o "DAEMON_VERSION = '[^']*'" scripts/daemon.js | head -1 | cut -d"'" -f2)
          fi
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "检测到版本: $VER"

      - name: 打包内置资产 builtin（build-win-zip.sh 需要）
        shell: bash
        run: |
          SRC="WorkDaddy.app/Contents/Resources/scripts/builtin"
          if [ -d "$SRC" ]; then
            cp -R "$SRC/." scripts/builtin/
            echo "已复制 builtin 资产"
          else
            echo "警告: 未找到 builtin，面板将无官方壁纸（不影响安装功能）"
          fi

      - name: 生成 Windows 安装暂存包（仅供 Inno Setup 使用）
        shell: bash
        env:
          WORKDADDY_BUILD_VERSION: ${{ steps.ver.outputs.version }}
        run: bash scripts/build-win-zip.sh

      - name: 安装 Inno Setup
        shell: pwsh
        run: |
          choco upgrade innosetup --version=6.7.1 --allow-downgrade --no-progress --yes
          if ($LASTEXITCODE -ne 0) { throw "Inno Setup installation failed with exit code $LASTEXITCODE" }

      - name: 生成 Setup.exe（用户态 Inno Setup 安装包）
        shell: pwsh
        run: |
          $version = (Select-String -Path scripts\daemon.js -Pattern "const DAEMON_VERSION = '([^']+)'" ).Matches[0].Groups[1].Value
          $iscc = Join-Path ${env:ProgramFiles(x86)} 'Inno Setup 6\ISCC.exe'
          # v0.3.0 起发布物收敛为全端单包（WorkSwitch-All-Setup-*），分身安装包下架
          powershell.exe -NoLogo -NoProfile -ExecutionPolicy RemoteSigned `
            -File .\scripts\build-win-installer.ps1 -IsccPath $iscc -Profile all -Version ${{ steps.ver.outputs.version }}
          if ($LASTEXITCODE -ne 0) { throw "WorkSwitch All Setup build failed with exit code $LASTEXITCODE" }

      - name: 确认仅保留 Setup.exe 发行产物
        shell: pwsh
        run: |
          $legacyZip = Get-ChildItem -LiteralPath release\windows -Filter '*-win64.zip' -File -ErrorAction SilentlyContinue
          if ($legacyZip) {
            $legacyZip | Remove-Item -Force
          }
          if (Get-ChildItem -LiteralPath release\windows -Filter '*-win64.zip' -File -ErrorAction SilentlyContinue) {
            throw 'Windows 发布目录仍包含旧 ZIP 产物。'
          }

      - name: 列出产物
        shell: bash
        run: ls -lh release/windows/

      - name: 产物完整性自检（verify-win.cmd，纯只读）
        shell: cmd
        run: |
          call scripts\verify-win.cmd

      - name: 上传产物（仅 Setup.exe）
        uses: actions/upload-artifact@v4
        with:
          name: workdaddy-win64
          path: |
            release/windows/WorkSwitch-All-Setup-*.exe

      # ---- 只有打 tag 时才顺手挂到 GitHub Release ----
      - name: 发布到 GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/windows/WorkSwitch-All-Setup-*.exe
            release/*.dmg
          generate_release_notes: true
